# =======================================================================
# Mario DRL — dependências travadas.
# Setup validado em Linux + Python 3.10/3.11 + uv.
# NÃO usar Python 3.12+ (nes-py 8.2.1 quebra com OverflowError de uint8).
# =======================================================================

# Core científico (numpy<2 é obrigatório por causa do nes-py)
numpy<2.0
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.65

# Deep learning
torch>=2.0,<2.5
tensorboard>=2.14

# RL: legado (gym) + moderno (gymnasium) coexistem; shimmy faz a ponte
gym==0.26.2
gymnasium==0.29.1
shimmy==1.3.0
stable-baselines3==2.3.2

# Super Mario Bros env
gym-super-mario-bros==7.4.0
nes-py==8.2.1

# Visualização
imageio>=2.30
opencv-python-headless>=4.8

# Jupyter (apenas para o notebook de análise)
ipykernel>=6.25
jupyterlab>=4.0
